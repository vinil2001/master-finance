@model IEnumerable<Finance.bank>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Банки</h2>

@*<p>
    @Html.ActionLink("Создать новый", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Название")
        </th>
        <th>
            @Html.DisplayName("Номер счета")
        </th>
        <th>
            @Html.DisplayName("МФО")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.chet)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mfob)
        </td>
        @*<td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.id }) |
            @Html.ActionLink("Просмотреть", "Details", new { id=item.id }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.id }) 
        </td>*@
    </tr>
}

</table>
